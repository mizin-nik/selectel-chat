{
  "name": "selectel-chat",
  "version": "1.0.0",
  "scripts": {
    "dep": "cd socket && npm i && cd ../chat-app && npm i",
    "socket": "cd socket && npm run start",
    "chat-app": "cd chat-app && npm run app:serve",
    "app:start": "npm-run-all --parallel socket chat-app"
  },
  "dependencies": {},
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
